<%= form_with(model: feel, local: true) do |form| %>
  <% if feel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feel.errors.count, "error") %> prohibited this feel from being saved:</h2>

      <ul>
      <% feel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id, id: :feel_post_id %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :feel_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
